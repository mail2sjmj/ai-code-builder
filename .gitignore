# ─── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
ENV/
.Python
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.mypy_cache/
.ruff_cache/

# ─── Node / Frontend ─────────────────────────────────────────────────────────
node_modules/
frontend/dist/
frontend/.vite/
*.tsbuildinfo

# ─── Environment files — ALL contain secrets — NEVER commit ─────────────────
# Base selector (.env): sets APP_ENV only.
.env
.env.local
backend/.env
frontend/.env

# Per-environment files (.env.development / .env.staging / .env.production):
# each is fully self-contained and includes the ANTHROPIC_API_KEY for that
# environment — must be gitignored.
# Use .env.example as the template; create one file per environment locally.
backend/.env.development
backend/.env.staging
backend/.env.production

# Personal local overrides (e.g. .env.development.local) — also gitignored:
.env.*.local
backend/.env.*.local
frontend/.env.*.local

# ─── IDE ─────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ─── Service management runtime files ───────────────────────────────────────
.pids/              # PID files written by scripts/manage.py at runtime

# ─── Docker ──────────────────────────────────────────────────────────────────
.docker/

# ─── Temp / uploads ──────────────────────────────────────────────────────────
/tmp/
*.csv
*.xlsx
*.parquet
# Exception: keep example files if any
!examples/
